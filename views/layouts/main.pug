doctype html
html
    head
        script(src="https://kit.fontawesome.com/f38dd034ab.js", crossorigin="anonymous")
        title #{pageTitle} | #{siteName}
    body
        include ../partials/header
        main
            block content
        include ../partials/footer